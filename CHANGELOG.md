# Changelog

## 2026-03-30
* 실시간 검색어 집계를 `viewlog` 원본 스캔에서 `pageview_daily` 일별 집계 테이블 기반으로 변경
* 익명 방문은 실시간 검색어 집계만 반영하고, 개인 열람 기록 `viewlog`에는 로그인 사용자만 저장하도록 조정
* 문서 보기에서 최근 변경/실시간 검색어 사이드바를 중복 계산하던 경로 제거
* 오래된 `viewlog`와 `pageview_daily` 데이터를 매일 정리하도록 유지보수 작업 갱신
